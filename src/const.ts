export const GRID_SIZE = 16;
export const SEGMENTS_PER_WIDTH = 20;

export type Vector2Array = [number, number];
export type Vector3Array = [number, number, number];

export enum Direction {
    LEFT,
    RIGHT,
    TOP,
    BOTTOM
}

